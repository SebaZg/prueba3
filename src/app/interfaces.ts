export interface Interfaces {
}
export interface Users {
    nombre: string;
    edad: number;
    rut: string;
    direccion: string;
    correo: string;
    telefono: number;
    contrasena: string;
}
